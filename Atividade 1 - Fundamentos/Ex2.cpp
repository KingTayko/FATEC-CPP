#include <iostream>

using namespace std;

int main()
{
    int b, h, a;

    cout << "Informe a base(b) do retangulo: ";
    cin >> b;

    cout << "Informe a altura(h) do retangulo: ";
    cin >> h;

    a = b * h;

    cout << "A area do retangulo e: " << a;

}